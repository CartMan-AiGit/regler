# ESPHome secrets — COPY THIS FILE to secrets.yaml and fill in your values!
# cp secrets.yaml.example secrets.yaml

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
ap_password: "boiler-fallback-123"

# Generate with: python3 -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
api_key: "YOUR_API_KEY_BASE64_HERE"

ota_password: "YOUR_OTA_PASSWORD"
